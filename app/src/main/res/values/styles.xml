<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimaryDark">@color/toolbar_orange</item>
        <item name="searchViewStyle">@style/AppSearchViewStyle</item>
        <item name="colorPrimary">#673AB7</item>
        <item name="colorAccent">#FF4081</item>

    </style>
    <style name="AppSearchViewStyle" parent="Widget.AppCompat.SearchView">
        <item name="android:textCursorDrawable">@color/textColor</item>
        <item name="queryBackground">@android:color/transparent</item>
        <item name="searchIcon">@drawable/ic_create_white_36dp</item>
        <item name="closeIcon">@drawable/ic_cached_white_36dp</item>
        <item name="queryHint">@string/ok</item>
        <item name="android:imeActionId">6</item>
    </style>

    <style name="AppTheme.Holo">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="CircularProgress" parent="AppTheme">
        <item name="colorAccent">@android:color/holo_orange_light</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

</resources>
